services:
  - type: web
    name: private-diary
    runtime: python
    plan: starter
    buildCommand: "true"
    startCommand: "python server.py"
    envVars:
      - key: DIARY_DATA_DIR
        value: /var/data
    disk:
      name: diary-data
      mountPath: /var/data
      sizeGB: 1
